# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.uv/

# Testes e cobertura
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.*
!.env.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
# `terraform plan -out=tfplan` sem extensão, como o runbook usa
tfplan
tfplan-*

# Material da Alup que não é versionado (brand book e afins)
*.pdf
!docs/**/*.pdf

# GCP
service-account-*.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Bandit
bandit-report.json

# Requirements exportado temporariamente pelo `make audit`
.requirements.txt

# Dataform
node_modules/
.df-credentials.json

# Worktrees criados por ferramenta de edicao dentro do repositorio.
# Sem esta linha, um `git add -A` distraido comita a arvore inteira de um
# worktree — que e uma copia completa do repositorio.
.claude/worktrees/
